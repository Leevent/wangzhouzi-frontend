# 望周知 - 系統架構文件

## 1. 架構概覽

本專案採用 **JAMstack** 架構，前端使用 Next.js，後端使用 Ghost 作為 Headless CMS。

```
┌─────────────────────────────────────────────────────────────┐
│                        使用者瀏覽器                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Next.js 前端 (Zeabur)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   App       │  │ Components  │  │    Lib      │         │
│  │   Router    │  │    UI       │  │   Ghost     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                Ghost CMS API (Zeabur)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   Posts     │  │    Tags     │  │   Authors   │         │
│  │  (資源)     │  │   (分類)    │  │   (作者)    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     MySQL 資料庫                              │
└─────────────────────────────────────────────────────────────┘
```

## 2. 核心設計原則

### 2.1 第一性原理：刪除重複

**問題**：原版每個頁面都複製貼上 80+ 行的導航列和頁尾程式碼。

**解決方案**：
- 使用 Next.js App Router 的 `layout.tsx` 統一管理佈局
- 建立 `Navbar` 和 `Footer` 元件，只寫一次
- 每個頁面只需關注自己的內容

### 2.2 元件化設計

```
components/
├── layout/          # 佈局元件（全站共用）
│   ├── Navbar.tsx   # 導航列（含響應式選單）
│   └── Footer.tsx   # 頁尾
│
└── ui/              # UI 元件（可重用）
    ├── Button.tsx       # 按鈕（支援 link/button 模式）
    ├── ResourceCard.tsx # 資源卡片（3 種變體）
    ├── CategoryCard.tsx # 分類卡片
    ├── PageHeader.tsx   # 頁面標題
    └── ScrollToTop.tsx  # 回到頂部按鈕
```

### 2.3 配置集中管理

```typescript
// src/config/site.ts - 網站配置
export const siteConfig = {
  name: '望周知',
  slogan: '希望每個人都能知道',
  contact: { email: '...', location: '...' },
  navigation: { main: [...], footer: [...] },
  copyrightYear: 2026,
};

// src/config/categories.ts - 分類樣式
export const categoryConfig = {
  '數位學習': { icon: '📚', color: '#E57373', description: '...' },
  // ...
};
```

## 3. 資料流

```
Ghost CMS (Posts/Tags)
        │
        ▼
  GhostService.ts
  ┌─────────────────────────────┐
  │ getAllResources()           │
  │ getResourcesByCategory()    │
  │ getFeaturedResources()      │
  │ getAllCategories()          │
  │ searchResources()           │
  │ getResourceBySlug()         │
  │ getBlogPosts()              │
  └─────────────────────────────┘
        │
        ▼
   Server Components
        │
        ▼
    UI Components
```

### 3.1 錯誤處理策略

所有 API 呼叫都有 fallback 機制：

```typescript
static async getAllResources(): Promise<Resource[]> {
  try {
    const posts = await api.posts.browse({ ... });
    return posts as Resource[];
  } catch (error) {
    console.error('Error fetching resources:', error);
    return FALLBACK_RESOURCES; // 返回預設資料
  }
}
```

## 4. 路由結構

| 路徑 | 說明 |
|------|------|
| `/` | 首頁 |
| `/resources` | 所有資源列表 |
| `/categories` | 分類瀏覽 |
| `/category/[slug]` | 單一分類的資源列表 |
| `/resource/[slug]` | 單一資源詳情 |
| `/search` | 搜尋頁面 |
| `/blog` | 部落格（即將推出）|
| `/about` | 關於我們 |
| `/guide` | 使用指南 |

## 5. 效能優化

1. **Server Components**: 所有頁面預設使用 Server Components，只在需要時使用 Client Components
2. **並行資料獲取**: 使用 `Promise.all()` 並行獲取多個 API
3. **圖片優化**: 使用 Next.js Image 元件
4. **字體優化**: 使用 Google Fonts 的 Noto Sans TC

## 6. 未來擴展

### 6.1 部落格功能
- 已預留 `/blog` 路由
- Ghost CMS 支援 `tag:blog` 過濾

### 6.2 訂閱功能
- 可整合 Ghost 的 Members 功能
- 或使用第三方電子報服務（如 Buttondown）

### 6.3 外部資源連結
- 已預留 `external-resources.ts` 配置檔位置
- 可擴展為獨立的「推薦資源」頁面

## 7. 版本控制

- **v1.0.0** (原版): 基本功能，程式碼重複度高
- **v2.0.0** (20260213): 重構版本
  - 統一 Layout 元件
  - 建立 UI 元件庫
  - 配置集中管理
  - 程式碼減少約 60%
